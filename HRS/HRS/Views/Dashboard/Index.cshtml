@model HRS.ViewModels.Dashboard.IndexViewModel;
@{
    ViewData["Title"] = "Dashboard!!";
}

<div>
    <h1>Welcome @(Model.Name)</h1>
    <h1>Successfully logged in</h1>
</div>
<div style="color:#FF0000">@(ViewData["3rdPartMessage"])</div>

@if (Model.Key is null)
{
    <p>Login with tracker? <a class="nav-link text-dark" href="Dashboard/LinkTracker">click here</a> to sign up and link account!</p>

}
